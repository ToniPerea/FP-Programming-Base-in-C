a) El operador logico && esta puesto en un caso el cual nunca se va a cumplir por lo cual
   no tiene ningun sentido su utilizacion ahi.

b) En este apartado ocurre una cosa pareciada al anterior, se usa el operador && en un caso
   que nunca se va a cumpliar ya que velocidad no puede ser mayor que 100 y menor que 70 a la vez.

c) En este apartado el operador logico || se esta usando con poco sentido ya que cuando se cumple el
   caso de que velocidad sea mayor que 100 automaticamente tambien se cumple el caso de que velocidad sea
   mayor que 110, por lo tanto no tiene sentido el uso del operador || aqui.
