#include <iostream>
#include <cmath>

using namespace std;

int main(){
   /*
   a) Para la edad de una persona usaria un variable de tipo entero.*/
   int edad;
   /*
   b) Para Producto interior bruto de un pais usaria una variable de tipo long ya que
      el numero no caberia en una variable de tipo entero al ser un numero tan grande.*/
   long int producto_interior_bruto;
   /*
   c) Para la cualidad de que un numero sea primo o no usaria una variable de tipo booleno.*/
   bool es_primo;
   /*
   d) Para el Estado civil de una persona usaria varibles de tipo booleano.*/
   bool esta_casado;
   bool esta_viudo;
   bool esta_soltero;
   bool esta_separado;
   /*
   e) Para saber si una persona es hombre o mujer usaria una variable de tipo booleano.*/
   bool es_hombre;
   //ó
   bool es_mujer;
}
